package com.marker;

import java.io.Serializable;

/**
 * Serializable is a marker interface, it is used when are transferring data over network. Serializable converts objects
 * into byte streams.
 * It is used to make sure that the state of the object doesn't change.
 */
public class SerializableDemo implements Serializable {
}
